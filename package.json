{
  "name": "medication-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Offline-first medication tracker for the web and iOS.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "ios:sync": "npm run build && cap sync ios",
    "ios:open": "npm run ios:sync && cap open ios",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/filesystem": "^8.0.0",
    "@capacitor/share": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "vite": "^7.0.0"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
